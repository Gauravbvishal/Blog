doctype html
html
  head
    title= blog.title
    style.
      body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        margin: 0;
        padding: 0;
        background: #f4f7fa;
        color: #333;
      }

      /* HEADER */
      header {
        background: linear-gradient(90deg, #0066ff, #00c6ff);
        color: white;
        padding: 20px;
        text-align: center;
        box-shadow: 0 3px 6px rgba(0,0,0,0.2);
      }
      header h1 {
        margin: 0;
        font-size: 28px;
        font-weight: bold;
      }

      /* CONTAINER */
      .container {
        width: 65%;
        margin: 40px auto;
        background: white;
        padding: 30px;
        border-radius: 12px;
        box-shadow: 0 6px 12px rgba(0,0,0,0.1);
      }
      .container h1 {
        margin-bottom: 20px;
        font-size: 26px;
        color: #0066ff;
      }
      .container p {
        color: #555;
        line-height: 1.7;
        font-size: 16px;
      }

      /* BACK BUTTON */
      a.back-btn {
        display: inline-block;
        margin-top: 25px;
        text-decoration: none;
        background: #0066ff;
        color: white;
        padding: 12px 20px;
        border-radius: 6px;
        font-size: 15px;
        font-weight: 500;
        transition: background 0.3s, transform 0.2s;
      }
      a.back-btn:hover {
        background: #004ccc;
        transform: translateY(-2px);
      }
      a.del-btn {
        display: inline-block;
        margin-top: 25px;
        margin-right:40px;
        text-decoration: none;
        background: #0066ff;
        color: white;
        padding: 12px 20px;
        border-radius: 6px;
        font-size: 15px;
        font-weight: 500;
        transition: background 0.3s, transform 0.2s;
      }
  body
    header
      include includes/header
       

    .container
      h1= blog.title
      p= blog.content
      image=blog.image
      
      a.back-btn(href="/showblog") ← Back to Blogs
    
    footer 
        include includes/footer
      
